/**
 * Abas no período Diário («Dias Corridos» / «Dias da Semana»).
 * false = seletor de intervalo abre direto, sem guia; só «Dias da Semana» fica oculto.
 */
export const SHOW_DIAS_DA_SEMANA_PERIOD_UI = false;

/** Período Anual: permite selecionar o ano corrente (ex.: 2026) fora do Intraday. */
export const ALLOW_CURRENT_YEAR_ANNUAL_SELECTION = true;
